<div class="container">
  <h1 class="text-center">Your game results</h1>
  <p>Your word : <%= @userword %></p>
  <% if @included %>
    <% if @english_word %>
      <p><strong>Congratulations!</strong> <%= @userword %> is a valid English word!</p>
    <% else %>
      <p>Sorry but <strong><%= @userword %></strong> does not seem to be a valid English word...</p>
    <% end %>
  <% else %>
    <p>Sorry but <strong><%= @userword %></strong> can't be built out of <%= @letters.join(", ") %></p>
  <% end %>
  <p>Your score is  <%= @compute_score %></p>
  <%= link_to "Play again", "/new" %>

</div>
